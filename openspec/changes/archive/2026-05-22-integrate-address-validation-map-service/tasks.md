## 1. 地图配置与后端基础能力

- [x] 1.1 在后端配置中新增地图服务配置项：provider、Web 服务 Key、请求超时、校验开关
- [x] 1.2 新增地图服务配置类，读取 `map.*` 或等价配置
- [x] 1.3 新增地址校验请求/响应 DTO，包含省、市、区、详细地址、完整地址、经度、纬度、校验状态和错误信息
- [x] 1.4 新增地图服务客户端接口，定义地理编码/地址解析方法
- [x] 1.5 实现高德地图 Web 服务客户端，调用地理编码 API 并解析经纬度、标准化地址和行政区信息
- [x] 1.6 处理地图服务未配置、网络异常、认证失败、配额限制等错误，并返回明确业务错误

## 2. 地址校验服务

- [x] 2.1 新增 `AddressValidationService` 或等价服务，统一组装结构化地址并调用地图客户端
- [x] 2.2 校验地图返回结果是否包含经纬度
- [x] 2.3 校验地图返回结果的省、市、区是否与用户提交字段一致或可归一匹配
- [x] 2.4 对多结果返回选择行政区匹配且精度最高的一条；无法确认时返回地址无效
- [x] 2.5 校验成功时返回 `VALID`、经纬度和标准化地址
- [x] 2.6 地址无匹配或行政区不一致时返回 400 业务错误
- [x] 2.7 地图服务调用失败时返回“地址校验暂不可用”，不得生成 `VALID` 结果

## 3. 店铺地址接入

- [x] 3.1 更新 `ShopService.register()`，保存前调用地址校验服务
- [x] 3.2 注册店铺地址校验成功后保存经纬度和 `addressValidationStatus=VALID`
- [x] 3.3 注册店铺地址校验失败时不创建店铺
- [x] 3.4 更新 `ShopService.update()`，编辑地址时重新校验并更新经纬度和校验状态
- [x] 3.5 编辑地址校验失败时保留原店铺地址和原经纬度
- [x] 3.6 确保店铺响应 DTO 返回经纬度和地址校验状态，兼容历史 `UNVERIFIED` 店铺

## 4. 订单收货地址接入

- [x] 4.1 更新 `OrderService.createOrder()`，扣减库存和保存订单前先校验收货地址
- [x] 4.2 收货地址校验成功后保存 `receiverLongitude`、`receiverLatitude` 和 `receiverAddressValidationStatus=VALID`
- [x] 4.3 收货地址校验失败时不创建订单、不扣减库存、不清空购物车
- [x] 4.4 地图服务暂不可用时不创建订单，并返回明确错误
- [x] 4.5 确保订单详情、卖家订单、管理员订单响应返回收货地址经纬度和校验状态
- [x] 4.6 确保历史订单缺少经纬度时仍按文本地址正常展示

## 5. 前端地图基础组件

- [x] 5.1 新增前端地图配置读取方式，例如 `VITE_AMAP_JS_API_KEY` 和必要安全配置
- [x] 5.2 新增高德 JS API 加载封装或可复用 composable，避免页面重复加载 SDK
- [x] 5.3 新增通用地图 marker 组件，支持传入经度、纬度、标题和地址文本
- [x] 5.4 地图组件处理未配置 Key、无经纬度、SDK 加载失败等降级状态
- [x] 5.5 地图组件在移动端和桌面端保持稳定高度，不影响页面主要操作

## 6. 前端表单与展示接入

- [x] 6.1 更新 `SellerShopView.vue`，店铺注册/编辑提交后展示地址校验状态和地图预览
- [x] 6.2 更新 `CheckoutView.vue`，下单时展示地址校验失败提示，并在成功后使用后端保存的经纬度
- [x] 6.3 更新 `ShopStorefrontView.vue`，店铺地址已校验且有经纬度时展示店铺地图
- [x] 6.4 更新 `OrderDetailView.vue`，收货地址已校验且有经纬度时展示收货地图
- [x] 6.5 更新 `SellerOrdersView.vue`，卖家订单有收货经纬度时展示或展开收货地图
- [x] 6.6 更新 `AdminOrders.vue`，管理员订单详情有收货经纬度时展示收货地图
- [x] 6.7 所有页面在没有经纬度或地图加载失败时继续展示文本地址

## 7. 后端自动化测试

- [x] 7.1 为地图客户端/地址校验服务添加成功地理编码测试
- [x] 7.2 为地址无匹配结果添加失败测试
- [x] 7.3 为行政区不一致添加失败测试
- [x] 7.4 为地图服务异常或未配置添加失败测试
- [x] 7.5 为 `ShopService.register()` 添加地址校验成功保存经纬度的测试
- [x] 7.6 为 `ShopService.register()` 添加地址校验失败不创建店铺的测试
- [x] 7.7 为 `ShopService.update()` 添加地址校验失败保留原地址的测试
- [x] 7.8 为 `OrderService.createOrder()` 添加地址校验成功保存收货经纬度的测试
- [x] 7.9 为 `OrderService.createOrder()` 添加地址校验失败不创建订单、不扣库存的测试

## 8. 验证命令与手动验收

- [x] 8.1 在 `backend/` 运行 `mvn.cmd test`
- [x] 8.2 在 `frontend/` 运行 `npm.cmd run build`
- [ ] 8.3 手动验证：未配置地图 Key 时，新地址提交返回地址校验暂不可用，历史地址仍可展示
- [ ] 8.4 手动验证：卖家注册有效店铺地址后，店铺保存为 `VALID` 并展示地图 marker
- [ ] 8.5 手动验证：卖家提交无效或行政区不匹配地址时，店铺注册/编辑被拒绝
- [ ] 8.6 手动验证：买家使用有效收货地址下单后，订单保存收货经纬度并展示地图 marker
- [ ] 8.7 手动验证：买家提交无效收货地址时，订单不创建、库存不扣减、购物车不清空
- [ ] 8.8 手动验证：店铺主页、买家订单详情、卖家订单页、管理员订单详情在地图可用/不可用时均能正常降级
