## 1. 后端会员数据模型

- [x] 1.1 新增会员套餐状态枚举 `MembershipPlanStatus`，包含 `ACTIVE`、`INACTIVE`
- [x] 1.2 新增买家会员状态枚举 `BuyerMembershipStatus`，包含 `ACTIVE`、`EXPIRED`
- [x] 1.3 新增会员购买状态枚举 `MembershipPurchaseStatus`，包含 `PENDING_PAYMENT`、`PAID`、`CANCELLED`、`EXPIRED`
- [x] 1.4 新增优惠券受众枚举或等价字段，区分普通优惠券和会员专属优惠券
- [x] 1.5 新增 `MembershipPlan` 实体，包含名称、说明、价格、有效期月数、折扣率、每月专属优惠券 ID 和启停状态
- [x] 1.6 新增 `BuyerMembership` 实体，包含买家 ID、套餐 ID、状态、开始时间、到期时间和最近支付时间
- [x] 1.7 新增 `MembershipPurchase` 实体，包含买家 ID、套餐 ID、支付金额、状态、支付流水号和支付完成时间
- [x] 1.8 新增 `MembershipMonthlyBenefit` 实体，记录买家、套餐、权益月份、优惠券模板 ID、买家优惠券 ID 和领取时间
- [x] 1.9 新增 repository，支持套餐启停查询、买家会员查询、购买记录幂等更新和月度权益唯一校验
- [x] 1.10 更新 `schema.sql`，新增会员相关表、订单会员快照字段、优惠券受众字段和必要索引/唯一约束

## 2. 后端会员套餐管理

- [x] 2.1 新增会员套餐创建/更新请求 DTO，校验名称、价格、有效期月数、折扣率和每月专属优惠券 ID
- [x] 2.2 新增会员套餐响应 DTO，返回套餐配置、启停状态和每月权益摘要
- [x] 2.3 新增 `MembershipAdminService`，支持创建、编辑、启用、停用和列表查询会员套餐
- [x] 2.4 校验套餐价格必须大于 0、有效期月数至少为 1、折扣率必须大于 0 且小于等于 1
- [x] 2.5 校验每月专属优惠券 ID 存在且符合会员专属券配置要求
- [x] 2.6 新增管理端会员套餐 controller，并仅允许 `ADMIN` 角色访问
- [x] 2.7 添加 service 测试：创建有效套餐成功
- [x] 2.8 添加 service 测试：无效价格、无效有效期、无效折扣率或不存在的专属券返回 400
- [x] 2.9 添加 service 测试：启用和停用套餐状态正确变化
- [x] 2.10 添加 controller/API 测试：非管理员访问套餐管理接口返回 403

## 3. 后端会员购买与支付

- [x] 3.1 新增买家会员套餐列表接口，只返回 `ACTIVE` 套餐
- [x] 3.2 新增买家会员购买接口，创建 `PENDING_PAYMENT` 会员购买记录
- [x] 3.3 会员购买发起支付时生成带业务类型前缀的支付流水号，避免与商品订单混淆
- [x] 3.4 扩展支付回调处理，能够根据业务类型将会员购买支付成功事件路由到会员服务
- [x] 3.5 实现会员支付成功激活逻辑：无会员或已过期时从支付成功时间开始计算有效期
- [x] 3.6 实现会员续费逻辑：有效会员从当前到期时间开始延长有效期
- [x] 3.7 实现支付回调幂等保护：同一会员购买记录重复回调不重复延长有效期
- [x] 3.8 禁止买家购买 `INACTIVE` 套餐，返回 400 且不创建购买记录
- [x] 3.9 添加 service 测试：首次购买支付成功创建有效会员
- [x] 3.10 添加 service 测试：有效会员续费不会丢失剩余有效期
- [x] 3.11 添加 service 测试：过期会员重新购买从本次支付成功时间开始
- [x] 3.12 添加 service 测试：重复支付回调只激活或延长一次
- [x] 3.13 添加 controller/API 测试：非买家访问会员购买接口返回 403

## 4. 后端会员状态与月度权益

- [x] 4.1 新增买家会员中心接口，返回当前会员状态、套餐、折扣率、到期时间和当月权益领取状态
- [x] 4.2 会员状态查询需要派生过期状态，过期会员不得继续使用会员折扣或领取会员专属券
- [x] 4.3 扩展优惠券模型和列表逻辑，确保会员专属券不出现在普通可领取优惠券列表中
- [x] 4.4 新增会员当月专属券领取接口，仅允许有效会员领取
- [x] 4.5 领取会员专属券时复用买家优惠券实例模型，创建 `UNUSED` 买家优惠券记录
- [x] 4.6 使用买家、套餐和自然月唯一约束防止同月重复领取
- [x] 4.7 套餐未配置专属券、专属券停用或专属券过期时返回明确错误
- [x] 4.8 添加 service 测试：有效会员领取当月专属券成功并生成买家优惠券
- [x] 4.9 添加 service 测试：同月重复领取不创建第二张买家优惠券
- [x] 4.10 添加 service 测试：非会员或过期会员不能领取会员专属券
- [x] 4.11 添加 service 测试：普通可领取优惠券列表不返回会员专属券
- [x] 4.12 添加并发或唯一约束测试：并发领取同月权益最多成功一次

## 5. 订单会员折扣集成

- [x] 5.1 更新 `Order` 实体和订单响应 DTO，新增会员套餐 ID、套餐名称、折扣率和会员优惠金额快照字段
- [x] 5.2 新增或扩展会员折扣计算服务，按买家有效会员返回折扣率和快照信息
- [x] 5.3 更新 `OrderService.createOrder()`，在优惠券抵扣后对剩余金额应用会员折扣
- [x] 5.4 确保 `Order.totalAmount` 表示优惠券和会员折扣后的实际支付金额，最终金额不小于 0
- [x] 5.5 非会员或过期会员下单时会员优惠金额为 0，且不保存有效会员快照
- [x] 5.6 会员下单后套餐后续修改或停用不影响订单会员快照
- [x] 5.7 库存不足、购物车为空、无效优惠券或地址校验失败时不创建订单、不占用优惠券、不保存会员优惠快照
- [x] 5.8 更新订单详情页和后台订单列表相关响应，展示会员优惠金额和会员套餐快照
- [x] 5.9 添加订单 service 测试：非会员下单不应用会员折扣
- [x] 5.10 添加订单 service 测试：有效会员下单自动应用折扣并保存会员快照
- [x] 5.11 添加订单 service 测试：会员折扣与优惠券叠加金额计算正确
- [x] 5.12 添加订单 service 测试：过期会员下单不应用会员折扣
- [x] 5.13 添加订单 service 测试：套餐后续修改不影响历史订单快照
- [x] 5.14 添加异常回滚测试：下单失败时不保存会员优惠快照、不占用优惠券

## 6. 前端会员管理与买家会员中心

- [x] 6.1 新增会员 API wrapper，覆盖管理端套餐管理、买家套餐列表、购买会员、会员状态和月度权益领取
- [x] 6.2 新增管理端会员套餐页面，支持创建、编辑、启用、停用套餐和配置每月专属券
- [x] 6.3 在管理后台导航中加入“会员管理”入口
- [x] 6.4 新增买家会员中心页面，展示会员状态、到期时间、套餐权益和当月专属券领取状态
- [x] 6.5 新增会员购买页面或在会员中心内展示可购买套餐，并支持发起支付
- [x] 6.6 在买家导航中加入“会员中心”入口
- [x] 6.7 在结算页展示会员折扣预览、优惠券抵扣、会员优惠金额和最终应付金额
- [x] 6.8 在订单详情页展示会员套餐名称、会员折扣率和会员优惠金额快照
- [x] 6.9 前端错误提示应展示服务端返回的具体原因，包括套餐停用、会员过期、同月已领取和支付未完成

## 7. 验证

- [x] 7.1 运行 `mvn.cmd test`
- [x] 7.2 运行 `npm.cmd run build`
- [x] 7.3 运行 `openspec validate add-membership-benefits`
- [ ] 7.4 手动验证：管理员创建并启用会员套餐，买家可在会员中心看到该套餐
- [ ] 7.5 手动验证：买家购买会员并完成支付后，会员中心显示 `ACTIVE` 和正确到期时间
- [ ] 7.6 手动验证：有效会员当月领取专属优惠券成功，重复领取被拒绝
- [ ] 7.7 手动验证：非会员或过期会员不能领取会员专属券，普通优惠券列表不显示会员专属券
- [ ] 7.8 手动验证：有效会员结算订单时展示会员优惠金额，提交后订单详情金额与后端计算一致
- [ ] 7.9 手动验证：会员折扣和优惠券同时使用时，优惠券抵扣、会员优惠和应付金额展示正确
- [ ] 7.10 手动验证：管理员停用套餐后，新买家不能购买，已有会员的有效期和历史订单快照不受影响
