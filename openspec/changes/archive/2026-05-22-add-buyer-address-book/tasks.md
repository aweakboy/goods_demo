## 1. 后端地址簿数据模型

- [x] 1.1 新增买家地址实体，包含买家 ID、收货人、手机号、省市区、详细地址、完整地址、经纬度、校验状态、标准化地址、默认标记和时间字段
- [x] 1.2 新增买家地址 repository，支持按买家查询、按买家和地址 ID 查询、统计买家地址数量、清除默认地址
- [x] 1.3 新增买家地址请求和响应 DTO，限制字段长度和手机号格式
- [x] 1.4 更新数据库建表或 schema，新增买家地址表和必要索引

## 2. 后端地址簿业务服务

- [x] 2.1 新增买家地址 service，支持列表、新增、编辑、删除和设置默认地址
- [x] 2.2 新增/编辑地址时调用 `AddressValidationService`，保存经纬度、完整地址和 `VALID` 状态
- [x] 2.3 实现第一条地址自动设为默认地址
- [x] 2.4 实现设置默认地址时同一买家只有一个默认地址
- [x] 2.5 实现地址数量上限，例如每个买家最多 20 条地址
- [x] 2.6 确保买家只能访问、编辑、删除和设置自己的地址

## 3. 后端 API 与下单接入

- [x] 3.1 新增买家地址 controller，提供地址列表、新增、编辑、删除和设置默认地址接口
- [x] 3.2 确保地址簿接口仅允许 BUYER 角色访问，非买家访问返回 403
- [x] 3.3 扩展 `OrderRequest`，支持可选 `addressId`
- [x] 3.4 更新 `OrderService.createOrder()`，提供 `addressId` 时从地址簿复制收货地址快照
- [x] 3.5 保留未提供 `addressId` 时的手动地址下单和服务端地址校验流程
- [x] 3.6 确保使用不存在地址返回 404，使用他人地址返回 403，且不创建订单、不扣库存、不清空购物车
- [x] 3.7 确保地址簿后续修改或删除不影响已创建订单的地址快照

## 4. 后端自动化测试

- [x] 4.1 添加买家地址 service 测试：新增地址成功保存经纬度和 `VALID` 状态
- [x] 4.2 添加买家地址 service 测试：地址校验失败不保存地址
- [x] 4.3 添加买家地址 service 测试：编辑地址重新校验并更新字段
- [x] 4.4 添加买家地址 service 测试：第一条地址自动默认、设置默认地址时取消其他默认地址
- [x] 4.5 添加买家地址 service 测试：删除地址不影响历史订单快照
- [x] 4.6 添加权限测试：买家不能访问、编辑、删除或设置其他买家的地址
- [x] 4.7 添加订单 service 测试：使用地址簿下单保存地址快照
- [x] 4.8 添加订单 service 测试：使用不存在地址或他人地址下单不创建订单、不扣库存、不清空购物车
- [x] 4.9 添加 controller/API 测试：买家地址接口正常访问，非买家访问返回 403

## 5. 前端地址簿能力

- [x] 5.1 新增买家地址 API wrapper，覆盖列表、新增、编辑、删除和设置默认地址
- [x] 5.2 新增地址管理页面或组件，展示地址列表、默认标记和地址操作
- [x] 5.3 新增地址表单组件，复用收货人、手机号、省市区、详细地址字段和校验规则
- [x] 5.4 在导航或买家相关入口中加入“我的地址”入口
- [x] 5.5 地址新增/编辑失败时展示服务端地址校验错误

## 6. 前端结算页接入

- [x] 6.1 结算页加载买家地址簿，存在默认地址时自动填入表单
- [x] 6.2 结算页支持选择地址并填入收货人、手机号、省市区和详细地址
- [x] 6.3 地址簿为空时保持当前手动填写地址流程
- [x] 6.4 选择地址下单时提交 `addressId`
- [x] 6.5 手动填写地址下单时继续提交结构化地址字段
- [x] 6.6 支持从结算页保存新地址，保存成功后刷新地址列表并可用于当前订单

## 7. 验证

- [x] 7.1 在 `backend/` 运行 `mvn.cmd test`
- [x] 7.2 在 `frontend/` 运行 `npm.cmd run build`
- [ ] 7.3 手动验证：买家新增有效地址后地址出现在地址簿并展示为 `VALID`
- [ ] 7.4 手动验证：买家提交无效地址时保存失败且地址簿不新增记录
- [ ] 7.5 手动验证：默认地址唯一，切换默认地址后结算页自动填入新的默认地址
- [ ] 7.6 手动验证：结算页选择地址后能创建订单，订单详情展示创建时的地址快照
- [ ] 7.7 手动验证：修改或删除地址簿地址后，历史订单地址不变化
- [ ] 7.8 手动验证：非买家账号无法访问地址簿接口或页面
