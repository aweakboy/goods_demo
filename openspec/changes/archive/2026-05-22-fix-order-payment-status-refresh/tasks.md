## 1. 问题定位与后端入账路径

- [x] 1.1 复核 `PaymentService.handleNotify()` 的验签、交易状态、订单查找、状态更新和返回值分支
- [x] 1.2 复核 `PaymentController.notify()` 是否能正确接收支付宝表单回调参数并返回 `success`/`failure`
- [x] 1.3 梳理“支付宝已扣款但订单仍待付款”的可能原因，并在代码中保留必要日志或错误信息
- [x] 1.4 抽出统一的订单入账方法，供 notify 和主动核验共同使用

## 2. 服务端主动支付核验

- [x] 2.1 为 `PaymentService` 增加支付宝交易查询能力，根据 `out_trade_no` 查询真实交易状态
- [x] 2.2 查询成功且交易状态为 `TRADE_SUCCESS` 或 `TRADE_FINISHED` 时，校验订单号和支付金额
- [x] 2.3 金额匹配且订单为 `PENDING_PAYMENT` 时，将订单更新为 `PAID` 并记录 `alipayTradeNo`
- [x] 2.4 已 `PAID` 订单重复核验时保持幂等并返回最新订单
- [x] 2.5 金额不匹配、交易未成功、订单已取消或其他非待付款状态时，不得更新为 `PAID`
- [x] 2.6 新增买家侧支付状态核验接口，校验订单归属后调用服务端主动核验并返回最新订单

## 3. 前端支付状态确认

- [x] 3.1 更新 `frontend/src/api/order.js`，增加支付状态核验接口调用
- [x] 3.2 更新 `PaymentResultView.vue`，根据 `orderId` 触发后端支付核验并展示真实订单状态
- [x] 3.3 当核验后仍为 `PENDING_PAYMENT` 时，结果页进行有界重试并显示“支付确认中”
- [x] 3.4 更新 `OrderDetailView.vue`，发起支付后在页面重新获得焦点或可见时触发支付核验/刷新
- [x] 3.5 支付状态确认为 `PAID` 后，订单详情页隐藏支付入口并展示已付款状态

## 4. 后端自动化测试

- [x] 4.1 为 notify 成功入账添加单元测试：`PENDING_PAYMENT` → `PAID`，记录 `alipayTradeNo`
- [x] 4.2 为重复 notify 或重复主动核验添加幂等测试：已 `PAID` 保持成功
- [x] 4.3 为主动交易查询成功添加单元测试：支付宝成功且金额匹配时更新为 `PAID`
- [x] 4.4 为金额不匹配添加单元测试：不得更新为 `PAID`
- [x] 4.5 为交易未成功或验签失败添加单元测试：不得更新订单状态
- [x] 4.6 为买家侧核验接口添加归属校验测试：非本人订单不得核验

## 5. 验证命令与手动验收

- [x] 5.1 在 `backend/` 运行 `mvn.cmd test`
- [x] 5.2 在 `frontend/` 运行 `npm.cmd run build`
- [ ] 5.3 手动验证：支付宝沙箱扣款成功且 notify 正常到达时，订单自动变为已付款
- [ ] 5.4 手动验证：模拟 notify 未及时落库时，支付结果页触发服务端核验后订单变为已付款
- [ ] 5.5 手动验证：从订单详情页发起支付后回到原页面，订单状态通过后端核验刷新为已付款
- [ ] 5.6 手动验证：支付成功后订单列表、卖家订单页、管理员订单页重新进入页面时显示已付款
