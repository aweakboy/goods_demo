## 1. 路径规划基础能力

- [x] 1.1 新增路径规划配置字段，包括启用开关、供应商、复用 Web 服务 Key、超时、规划策略和最小刷新间隔
- [x] 1.2 新增路径规划 DTO，包括请求、路线点、标准化路线结果和供应商失败详情
- [x] 1.3 新增 `RoutePlanningClient` 接口和高德/AMap 路径规划实现
- [x] 1.4 添加客户端测试，覆盖成功路线解析、空路线响应、供应商失败状态、HTTP 异常和超时处理

## 2. 路线快照持久化

- [x] 2.1 新增路线规划状态枚举，包含可用、失败和跳过状态
- [x] 2.2 新增 `ShipmentRouteSnapshot` 实体或等价持久化模型，保存 shipment ID、供应商、坐标、polyline JSON、距离、耗时、规划时间、刷新时间和失败原因
- [x] 2.3 新增按 shipment ID 查询和保存路线快照的 repository 方法
- [x] 2.4 添加持久化测试，覆盖路线快照创建、更新和读取

## 3. 规划服务与地图响应

- [x] 3.1 实现 `ShipmentRoutePlanningService`，覆盖发货时规划、跳过快照、失败快照和成功快照
- [x] 3.2 确保路径规划失败不会回滚发货记录创建或订单状态更新
- [x] 3.3 扩展物流地图响应，包含路线来源、完整路线、已完成路线、距离、耗时、规划状态、规划时间和规划失败原因
- [x] 3.4 更新地图模拟逻辑，优先使用可用路线快照，缺失时回退到现有模拟路线
- [x] 3.5 实现沿路线 polyline 计算当前位置和已完成路线段，覆盖 SHIPPED、IN_TRANSIT、OUT_FOR_DELIVERY、DELIVERED 和 EXCEPTION 状态
- [x] 3.6 添加服务测试，覆盖路线成功、缺少坐标跳过、关闭路径规划跳过、供应商失败降级和基于路线的位置计算
- [x] 3.7 添加测试证明买家、卖家和管理员订单读取路径不会调用外部路径规划客户端

## 4. 发货与管理员接口接入

- [x] 4.1 在卖家创建 shipment 后、路径规划启用且坐标完整时，只调用一次路径规划
- [x] 4.2 在买家订单详情、卖家订单列表/详情和管理员订单详情的 shipment 响应中包含最新路线快照数据
- [x] 4.3 新增管理员专用接口或操作，用于刷新单个 shipment 的路线快照且不改变物流状态
- [x] 4.4 实现最小刷新间隔保护，重复刷新时拒绝请求且不调用外部供应商
- [x] 4.5 手动刷新失败时保留旧的可用路线快照
- [x] 4.6 添加 API 测试，覆盖发货创建路线快照、供应商失败时仍可发货、管理员刷新成功、刷新冷却、非管理员刷新返回 403，以及刷新失败保留旧路线

## 5. 前端路线展示

- [x] 5.1 扩展 `ShipmentMap.vue`，将 routePath 渲染为完整路线，将 completedPath 渲染为已完成路线段
- [x] 5.2 在物流地图附近展示路线来源、距离、预计耗时、规划状态和规划失败原因
- [x] 5.3 保留现有地图降级行为，覆盖未配置 JS API Key、SDK 加载失败、路线不可用、历史物流单号和缺少坐标
- [x] 5.4 更新买家订单详情：有缓存真实路线时展示真实路线，不可用时展示模拟路线
- [x] 5.5 更新卖家订单管理：展示缓存路线，页面加载不得触发路线刷新
- [x] 5.6 更新管理员订单详情：展示路线状态并提供受保护的刷新路线操作
- [x] 5.7 确保管理员刷新后更新路线、距离、耗时和当前位置，不重新加载无关订单数据

## 6. 验证

- [x] 6.1 在 `backend/` 运行 `mvn.cmd test`
- [x] 6.2 在 `frontend/` 运行 `npm.cmd run build`
- [ ] 6.3 手动验证：卖家使用有效坐标发货后只保存一个路线快照，订单详情读取该快照且不会二次调用供应商
- [ ] 6.4 手动验证：供应商失败或缺少路线配置时仍可创建 shipment，并展示模拟路线或降级原因
- [ ] 6.5 手动验证：买家订单详情展示真实路线、已完成路线段、当前位置 marker、距离和预计耗时
- [ ] 6.6 手动验证：卖家订单管理展示路线数据，且路径规划不可用时物流操作仍可使用
- [ ] 6.7 手动验证：管理员刷新路线成功后页面更新路线，刷新冷却能阻止重复调用
- [ ] 6.8 手动验证：历史物流单号订单和缺坐标 shipment 不显示空白地图
