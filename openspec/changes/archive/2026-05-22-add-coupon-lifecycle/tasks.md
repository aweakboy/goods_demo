## 1. 后端优惠券数据模型

- [x] 1.1 新增优惠券状态枚举 `CouponStatus`，包含 `DRAFT`、`ACTIVE`、`INACTIVE`
- [x] 1.2 新增买家优惠券状态枚举 `BuyerCouponStatus`，包含 `UNUSED`、`USED`、`EXPIRED`
- [x] 1.3 新增 `Coupon` 实体，包含名称、说明、门槛金额、抵扣金额、发放总量、已领取数量、每人领取上限、有效期、状态、创建人和时间字段
- [x] 1.4 新增 `BuyerCoupon` 实体，包含买家 ID、优惠券 ID、状态、领取时间、使用时间和使用订单 ID
- [x] 1.5 新增 `CouponRepository`，支持管理端分页查询、按状态/有效期查询可领取优惠券、领取时加锁或条件更新库存
- [x] 1.6 新增 `BuyerCouponRepository`，支持按买家查询、按买家和状态查询、统计买家领取次数、按 ID 和买家查询
- [x] 1.7 更新 `schema.sql`，新增优惠券表、买家优惠券表、必要索引和订单优惠券快照字段

## 2. 后端优惠券 DTO 与服务

- [x] 2.1 新增管理端优惠券创建/更新请求 DTO，校验名称、金额、数量、每人领取上限和有效期
- [x] 2.2 新增优惠券响应 DTO，返回优惠券配置、剩余数量、有效期和状态
- [x] 2.3 新增买家优惠券响应 DTO，返回买家优惠券状态、有效期、门槛金额、抵扣金额和使用订单 ID
- [x] 2.4 新增 `CouponAdminService`，支持创建、编辑、启用、停用和管理端列表查询
- [x] 2.5 新增 `BuyerCouponService`，支持可领取列表、领取优惠券、我的优惠券列表和状态派生
- [x] 2.6 在领取优惠券时校验 `ACTIVE`、有效期、剩余数量和每人领取上限
- [x] 2.7 在领取优惠券时实现并发库存保护，避免 `claimedQuantity` 超过 `totalQuantity`
- [x] 2.8 在买家优惠券列表中将已过期且未使用的优惠券展示为 `EXPIRED`

## 3. 后端 API 与权限

- [x] 3.1 新增管理端优惠券 controller，提供创建、编辑、启用、停用和列表接口
- [x] 3.2 确保管理端优惠券接口仅允许 `ADMIN` 角色访问，非管理员返回 403
- [x] 3.3 新增买家优惠券 controller，提供可领取列表、领取优惠券和我的优惠券列表接口
- [x] 3.4 确保买家优惠券接口仅允许 `BUYER` 角色访问，非买家返回 403
- [x] 3.5 为领取失败场景返回明确错误信息，包括已领完、已达领取上限、优惠券已停用和优惠券已过期

## 4. 后端订单接入

- [x] 4.1 扩展 `OrderRequest`，支持可选 `buyerCouponId`
- [x] 4.2 扩展 `Order` 实体和订单响应，保存 `originalAmount`、`discountAmount`、`couponId`、`buyerCouponId`、`couponName`、`couponThresholdAmount`、`couponDiscountAmount`
- [x] 4.3 更新 `OrderService.createOrder()`，先计算商品原始合计金额，再校验并应用买家优惠券
- [x] 4.4 确保 `Order.totalAmount` 表示优惠后的实际支付金额，未使用优惠券时优惠金额为 0
- [x] 4.5 使用优惠券下单成功后，将买家优惠券标记为 `USED` 并记录使用订单 ID
- [x] 4.6 使用不存在、不属于自己、已使用、已过期、停用或未达门槛的优惠券下单时返回对应错误
- [x] 4.7 确保无效优惠券、库存不足或购物车为空时不创建订单、不扣库存、不清空购物车、不标记优惠券为已使用
- [x] 4.8 确保优惠券后续修改或停用不影响历史订单优惠券快照
- [x] 4.9 更新支付、退款和后台统计相关逻辑，继续使用优惠后的 `totalAmount`

## 5. 后端自动化测试

- [x] 5.1 添加优惠券管理 service 测试：创建有效优惠券成功
- [x] 5.2 添加优惠券管理 service 测试：无效金额、无效数量或无效有效期返回 400
- [x] 5.3 添加优惠券管理 service 测试：启用和停用优惠券状态正确变化
- [x] 5.4 添加买家领取 service 测试：领取成功创建 `UNUSED` 买家优惠券并增加已领取数量
- [x] 5.5 添加买家领取 service 测试：已领完、超过每人领取上限、停用、过期场景不创建领取记录
- [x] 5.6 添加买家领取并发或边界测试：已领取数量不能超过发放总量
- [x] 5.7 添加买家优惠券列表测试：只返回当前买家的优惠券并正确展示过期状态
- [x] 5.8 添加订单 service 测试：使用优惠券下单保存原始金额、优惠金额、实际支付金额和优惠券快照
- [x] 5.9 添加订单 service 测试：使用他人优惠券、不存在优惠券、已使用优惠券、过期优惠券或未达门槛时不创建订单、不扣库存、不清空购物车、不占用券
- [x] 5.10 添加订单 service 测试：库存不足时优惠券不被标记为已使用
- [x] 5.11 添加 controller/API 测试：管理员优惠券接口非管理员返回 403
- [x] 5.12 添加 controller/API 测试：买家优惠券接口非买家返回 403

## 6. 前端优惠券管理与买家领取

- [x] 6.1 新增优惠券 API wrapper，覆盖管理端创建、编辑、启用、停用、列表，以及买家可领取、领取、我的优惠券列表
- [x] 6.2 新增管理端优惠券页面，支持创建满减券、查看剩余数量、启用和停用
- [x] 6.3 在管理后台导航中加入“优惠券管理”入口
- [x] 6.4 新增买家优惠券页面，展示可领取优惠券和我的优惠券
- [x] 6.5 在买家导航中加入“我的优惠券”入口
- [x] 6.6 领取优惠券失败时展示服务端返回的具体原因

## 7. 前端结算页与订单展示

- [x] 7.1 结算页加载当前买家可用于下单的 `UNUSED` 优惠券
- [x] 7.2 结算页支持选择和取消选择优惠券，并展示原始金额、优惠金额和应付金额预览
- [x] 7.3 选择优惠券下单时提交 `buyerCouponId`
- [x] 7.4 未选择优惠券时保持现有下单流程和金额展示
- [x] 7.5 订单详情页展示订单原始金额、优惠金额、实际支付金额和优惠券名称快照
- [x] 7.6 优惠券不可用、过期或未达门槛时在结算页给出明确提示

## 8. 验证

- [x] 8.1 在 `backend/` 运行 `mvn.cmd test`
- [x] 8.2 在 `frontend/` 运行 `npm.cmd run build`
- [x] 8.3 运行 `openspec validate add-coupon-lifecycle`
- [ ] 8.4 手动验证：管理员创建一张有效满减券后，买家可领取列表出现该券并显示剩余数量
- [ ] 8.5 手动验证：买家领取优惠券后，该券出现在“我的优惠券”中且状态为 `UNUSED`
- [ ] 8.6 手动验证：同一买家达到领取上限后再次领取失败，并展示服务端错误原因
- [ ] 8.7 手动验证：买家在结算页选择符合门槛的优惠券后，应付金额正确减少并能创建订单
- [ ] 8.8 手动验证：订单详情展示创建订单时的优惠券名称、优惠金额和实际支付金额快照
- [ ] 8.9 手动验证：管理员停用优惠券后，买家不能继续领取或在新订单中使用该券
- [ ] 8.10 手动验证：非管理员不能访问管理端优惠券接口或页面，非买家不能访问买家优惠券接口或页面
