## 1. 数据模型与迁移

- [ ] 1.1 在 `Coupon` 实体新增 `stackable` 字段，默认值为 `false`
- [ ] 1.2 更新 `schema.sql`，为 `coupons` 增加 `stackable` 列并给历史数据默认不可叠加
- [ ] 1.3 新增订单优惠券使用明细实体，例如 `OrderCouponUsage`
- [ ] 1.4 新增订单优惠券使用明细表，包含订单、买家优惠券、优惠券模板、适用人群、叠加配置和金额快照字段
- [ ] 1.5 新增 `OrderCouponUsageRepository`，支持按订单查询和批量保存
- [ ] 1.6 明确旧订单兼容策略，保留既有单券字段读取能力，新订单以明细表为准

## 2. 优惠券管理接口

- [ ] 2.1 在 `CouponRequest` 增加 `stackable` 字段，未传时按 `false` 处理
- [ ] 2.2 在 `CouponResponse` 增加 `stackable` 字段
- [ ] 2.3 更新 `CouponAdminService.create()` 保存叠加配置
- [ ] 2.4 更新 `CouponAdminService.update()` 支持修改叠加配置
- [ ] 2.5 更新买家可领取、我的优惠券和可用优惠券响应，返回叠加配置
- [ ] 2.6 确保已创建订单的优惠券快照不随优惠券模板叠加配置修改而变化

## 3. 下单优惠券校验与金额计算

- [ ] 3.1 在 `OrderRequest` 新增 `buyerCouponIds`，限制最多 2 个 ID
- [ ] 3.2 保留旧字段 `buyerCouponId` 单券兼容路径
- [ ] 3.3 同时提交 `buyerCouponId` 和 `buyerCouponIds` 时返回 400
- [ ] 3.4 在 `BuyerCouponService` 新增多券准备方法，锁定并校验所选买家优惠券
- [ ] 3.5 实现单券使用不要求 `stackable=true`
- [ ] 3.6 实现两券组合必须一张 `PUBLIC` 和一张 `MEMBER`
- [ ] 3.7 实现两券组合必须两张券都配置为允许叠加
- [ ] 3.8 实现两券均按订单商品原始金额校验满减门槛
- [ ] 3.9 实现两券总抵扣封顶，不允许订单金额被抵扣为负数
- [ ] 3.10 更新 `OrderService.createOrder()`，按优惠券总抵扣后金额继续计算会员折扣
- [ ] 3.11 创建订单成功后批量标记所选买家优惠券为 `USED`
- [ ] 3.12 创建订单成功后写入全部订单优惠券使用明细

## 4. 订单展示、取消与退款

- [ ] 4.1 扩展买家订单详情响应，返回优惠券使用明细列表
- [ ] 4.2 扩展管理员订单详情/列表响应，返回优惠券使用明细列表
- [ ] 4.3 更新订单详情前端展示多张优惠券明细和优惠券总抵扣
- [ ] 4.4 更新待付款订单取消逻辑，释放该订单占用的全部买家优惠券
- [ ] 4.5 更新支付超时自动取消逻辑，释放该订单占用的全部买家优惠券
- [ ] 4.6 更新退款批准成功逻辑，释放该订单占用的全部买家优惠券
- [ ] 4.7 确保退款申请、退款拒绝和支付渠道退款失败不释放优惠券
- [ ] 4.8 确保多券释放逻辑幂等，不修改其他订单占用的优惠券

## 5. 前端管理与结算体验

- [ ] 5.1 在管理员优惠券列表展示“允许叠加”状态
- [ ] 5.2 在管理员创建/编辑优惠券表单增加“允许叠加使用”开关
- [ ] 5.3 在买家优惠券列表展示优惠券是否可叠加
- [ ] 5.4 在结算页区分普通优惠券和会员专属券选择
- [ ] 5.5 在结算页只允许选择一张普通券和一张会员专属券
- [ ] 5.6 在结算页根据叠加配置禁用不合法组合，并展示明确原因
- [ ] 5.7 在结算页展示两张券的抵扣明细、优惠券总抵扣、会员折扣和最终应付金额
- [ ] 5.8 创建订单请求改为提交 `buyerCouponIds`，单券场景也走新字段
- [ ] 5.9 保留没有可叠加会员券时的单券下单体验

## 6. 后端自动化测试

- [ ] 6.1 添加 `CouponAdminServiceTest`：创建优惠券默认不可叠加
- [ ] 6.2 添加 `CouponAdminServiceTest`：创建和编辑优惠券可保存 `stackable=true`
- [ ] 6.3 添加优惠券 controller/API 测试：管理端响应包含 `stackable`
- [ ] 6.4 添加 `BuyerCouponServiceTest`：单张不可叠加优惠券可单独使用
- [ ] 6.5 添加 `BuyerCouponServiceTest`：普通券和会员券均允许叠加时通过校验
- [ ] 6.6 添加 `BuyerCouponServiceTest`：任一券不允许叠加时返回 400
- [ ] 6.7 添加 `BuyerCouponServiceTest`：两张同 audience 优惠券返回 400
- [ ] 6.8 添加 `BuyerCouponServiceTest`：任一券未达门槛、过期、已使用或不属于当前买家时拒绝下单
- [ ] 6.9 添加 `OrderServiceTest`：单券旧字段兼容创建订单成功
- [ ] 6.10 添加 `OrderServiceTest`：同时提交新旧优惠券字段返回 400
- [ ] 6.11 添加 `OrderServiceTest`：两券叠加后保存优惠券明细和总抵扣
- [ ] 6.12 添加 `OrderServiceTest`：两券抵扣后继续正确计算会员折扣
- [ ] 6.13 添加 `OrderServiceTest`：两券抵扣金额封顶不产生负订单金额
- [ ] 6.14 添加 `OrderServiceTest`：库存不足或地址校验失败时不标记任何优惠券为已使用
- [ ] 6.15 添加 `OrderServiceTest`：取消待付款订单退回全部优惠券
- [ ] 6.16 添加 `OrderServiceTest`：退款批准成功退回全部优惠券
- [ ] 6.17 添加 `OrderServiceTest`：退款拒绝和支付渠道退款失败不退回优惠券
- [ ] 6.18 添加多券释放幂等测试，确认不修改其他订单占用的买家优惠券

## 7. 前端验证与构建

- [ ] 7.1 运行 `mvn.cmd test`
- [ ] 7.2 运行 `npm.cmd run build`
- [ ] 7.3 运行 `openspec validate add-coupon-stackability-setting`
- [ ] 7.4 手动验证：管理员创建普通券和会员专属券时可配置“允许叠加使用”
- [ ] 7.5 手动验证：结算页选择不可叠加组合时按钮不可提交或展示明确错误
- [ ] 7.6 手动验证：结算页选择可叠加普通券和会员券后，订单金额明细正确
- [ ] 7.7 手动验证：取消待付款订单后，两张券都回到可用状态
- [ ] 7.8 手动验证：退款批准成功后，两张券都回到可用状态
